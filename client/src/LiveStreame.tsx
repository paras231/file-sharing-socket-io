import React, { useRef, useContext, useEffect } from "react";
import { SocketContext } from "./SocketContext";
// import Peer from "simple-peer"
const LiveStreame = () => {
  const { socket, streamData } = useContext(SocketContext);
  
  return (
    <>
      
    </>
  );
};

export default LiveStreame;
